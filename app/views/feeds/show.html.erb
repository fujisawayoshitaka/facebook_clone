<p id="notice"><%= notice %></p>

<p>
  <strong>写真:</strong>
  <% unless @feed.image.blank?%>
  <td><%= image_tag(@feed.image.url)  %></td>
  <% end %>
</p>
<p>本文:<%= @feed.content %></p>
<%= link_to '編集する', edit_feed_path(@feed) %> |
<%= link_to 'blog一覧へ戻る', feeds_path %>
